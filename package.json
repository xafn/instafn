{
    "name": "instafn",
    "version": "1.1.0",
    "description": "Feature-rich mod for Instagram web, including interaction confirms, following analyzer, and disabling seen receipts.",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "postbuild": "mkdir -p dist/settings && cp -R src/settings/. dist/settings/ 2>/dev/null || true",
        "preview": "vite preview"
    },
    "devDependencies": {
        "sharp": "^0.34.5",
        "vite": "^5.0.0",
        "vite-plugin-chrome-extension": "^0.0.7"
    }
}
